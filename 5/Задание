Исправить 4:
    + мьютексы по ссылке
    + возврат значения bool в get
    + мьютексы доделать места с RW
    + any в ключах нельзя - нужно использовать comparable
    + параметры кеша задавать через функциональные опции
    - выделить интерфейс Cache
        чет пока совсем не понимаю, как это сделать и зачем:
            - Если мы выделяем интерфейс Cache - то у него должно быть 2 метода Set \ Get, возможно Len
            - Эти два метода должны содержать одинаковую сигнатуру для LRU и TTL
            - При этом, если мы делаем TTL - то мы можем для отдельного элемента по Set указать своё время хранения
            - Но если это сделать даже через opts... - то сигнатуры метода отличаются
            - Единственный вариант для TTL сделать отдельный SetIndividual метод, в котором можно указать время хранения
            - Но тогда Cache будет кастрированным, зачем он в целом такой нужен?