- Реализовать LRU-cache
    В конструкторе указываем размер кеша, старые записи должны удаляться по логике LRU
- Реализовать TTL-cache
    В конструкторе указываем TTL, старые записи должны удаляться по TTL

    Отличие от предыдущего, в том, что, мы задаем время, в течении которого считаем данные валидными. Если они не валидны - то удаляем
    их. Не считать TTL как шаги - потому что это относится к вытеснению данных.

- Реализовать структуру данных Set (множество уникальных значений)
    Методы: "Пересечение двух множеств", "Объединение двух множеств", "Вычитание множества"
    
- Покрыть функционал тестами
